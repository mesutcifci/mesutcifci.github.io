import styled from "styled-components";
import { pagePadding } from "../../styles/globalStyles";

export const Wrapper = styled.main`
  display: flex;
  flex-direction: column;
  row-gap: 60px;
  padding: ${pagePadding};
`;
